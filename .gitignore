# =============================================================================
# cpu_54 / Vivado 2016.2 工程 —— 忽略中间产物与本地临时文件
#
# 说明：
#   1. 本文件只影响"尚未被 Git 跟踪"的文件，不会删除任何已存在的文件，
#      也不会影响本机 Vivado 工程继续使用。
#   2. 本仓库历史上提交过一部分 Vivado 生成目录（cpu_54.runs / cpu_54.sim /
#      cpu_54.cache / cpu_54.hw / cpu_54.ip_user_files / xsim.dir 等）。
#      这些目录已被下面的规则忽略；若希望它们"不再占用仓库体积"，
#      可在确认无需回溯这些历史产物后执行（仅从版本库移除索引，本地文件保留）：
#         git rm -r --cached cpu_54.runs cpu_54.sim cpu_54.cache cpu_54.hw cpu_54.ip_user_files xsim.dir
#         git commit -m "chore: untrack Vivado generated directories"
# =============================================================================

# -----------------------------------------------------------------------------
# Vivado 工程生成目录（重新打开工程或重跑脚本即可再生）
# -----------------------------------------------------------------------------
*.runs/
*.sim/
*.cache/
*.hw/
*.ip_user_files/
*.gen/
*.sdk/
*.srcs/utils/
.Xil/
xsim.dir/
.webtalk/

# -----------------------------------------------------------------------------
# Vivado / XSim 日志与运行状态文件
# -----------------------------------------------------------------------------
*.jou
*.log
*.debug
*.pb
*.str
*.wdf
*.wpc
*.rpt
usage_statistics_webtalk.html
usage_statistics_webtalk.xml
webtalk_pa.xml

# -----------------------------------------------------------------------------
# 综合 / 实现产物（checkpoint、网表、比特流、调试探针）
# -----------------------------------------------------------------------------
*.dcp
*.bit
*.rbt
*.mcs
*.ltx

# -----------------------------------------------------------------------------
# 仿真产物（XSim / ModelSim / QuestaSim）
# -----------------------------------------------------------------------------
*.wdb
*.wcfg
*.wlf
*.vcd
*.qdb
*.qpg
*.qtl
*.udo
*_info
_vmake

# -----------------------------------------------------------------------------
# IP / 工程临时目录
# -----------------------------------------------------------------------------
tmp/imem_ip_repair/
tmp/xsim*/

# -----------------------------------------------------------------------------
# Python（文档与图形生成脚本）
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# -----------------------------------------------------------------------------
# 编辑器 / 操作系统
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
